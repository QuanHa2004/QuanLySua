spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/quanlysua
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  # Chuỗi bí mật ngẫu nhiên (Hãy đổi chuỗi này khi deploy thực tế)
  secret: "9a4f2c8d3b7a1e5f6c8d9a0b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d"
  access-token-expiration: 900000    # 15 phút (tính bằng mili-giây)
  refresh-token-expiration: 604800000 # 7 ngày (tính bằng mili-giây)
